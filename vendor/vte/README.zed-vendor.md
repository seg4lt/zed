Local vendor fork of `vte` for Zed terminal notification parsing.

Origin:
- Crate: `vte`
- Version: `0.15.0` (crates.io source)

Local changes:
- Adds handler callback for terminal desktop notification OSC sequences (`9`, `99`, `777`).

